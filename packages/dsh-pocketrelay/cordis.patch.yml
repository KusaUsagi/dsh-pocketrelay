# dsh-pocketrelay — 手机远程连接
# 运行时配置可由 设置 → 手机连接 页覆盖（持久化于 storages/dsh-pocketrelay/config.json）。
- insert:
    - id: dsh-pocketrelay
      name: dsh-pocketrelay
      config:
        relayUrl: ""
        hostToken: ""
        autoConnect: true