{
  "name": "@dsh-pocketrelay/relay-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted relay for dsh-pocketrelay: pairing, mobile UI + structured data plane, admin console.",
  "main": "./dist/index.js",
  "bin": {
    "dsh-pocketrelay-relay": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/cli.js",
    "test": "node --test"
  },
  "license": "MIT",
  "dependencies": {
    "@dsh-pocketrelay/protocol": "workspace:*",
    "selfsigned": "^5.5.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "@types/ws": "^8.18.1"
  }
}
