{
  "name": "dsh-pocketrelay-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Self-hosted relay + DeepSeek Harness desktop plugin for phone-browser remote access over a tunnel.",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "esbuild": ">=0.20.0",
    "typescript": "^5.6.3"
  }
}
